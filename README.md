
# Form Detect URL

Detects all HTML form elements (action, method, input fields) in a given URL and prints them to the terminal with colored output. It also generates a detailed HTML report.

## Usage

```bash
python detect.py
```

## Requirements

```bash
python -m pip install requests beautifulsoup4 colorama
```

## Coded By Umut Özen
